# Engineering Review Plan

Slug: SLM_PAPER-20260905-1726
Date: 2026-09-05
Approach chosen: Option 1 (Safety-first hardening)

## Scope (requested)
1) Larger split for Block B (n>=30)
2) Tighter capability guard (lower allowed control drop)
3) Re-export updated benchmark table with CIs

## Proposed implementation tasks (no weight training)

### Task 1 — Hardening run profile for `intervene_margin.py`
- Add/run preset profile `safe_hardening`:
  - `n=30`
  - `max_control_drop` in [0.10, 0.15, 0.20]
  - `norm_budget` in [2.0, 3.0, 4.0]
  - conservative `steps` [8, 12]
  - conservative `lr` [0.03, 0.05]
  - stronger penalties: `break_penalty>=0.35`, `control_penalty>=0.35`
- Record best dev config + test metrics.

### Task 2 — Add CIs for Block B outputs
- In `intervene_margin.py` add bootstrap CIs for:
  - STEER
  - BREAK
  - control_drop (optional but recommended)
- Save CI fields in output JSON.

### Task 3 — Standardized benchmark export
- Update `RESULTS.md` section: `Decision-level intervention benchmark`
- Include rows for:
  - Block A methods (alpha sweep)
  - Block B hardening result
  - random baseline
- Required columns:
  - STEER mean + CI
  - BREAK mean + CI
  - logit lift mean + std
  - control_drop
  - semantic collapse rate

## Quality gates
- Gate G1: No background concurrent heavy MPS jobs.
- Gate G2: `intervene_margin` run completes at n>=30.
- Gate G3: BREAK reduced relative to previous 66.7% while retaining non-trivial STEER.
- Gate G4: `RESULTS.md` benchmark section updates without schema drift.

## Risks and mitigations
- Risk: STEER drops too much under tighter guard.
  - Mitigation: two-tier objective sweep and Pareto report.
- Risk: Runtime exceeds local limits.
  - Mitigation: serial runs + reduced candidate grid + checkpointed JSON.
- Risk: high variance due small test split.
  - Mitigation: bootstrap CI and report n explicitly.

## Deliverables
- `results/intervene_margin_*_intermediate.json` (hardened)
- `results/random_intervention_baseline_intermediate.json` (already available)
- Updated `RESULTS.md` with benchmark + interpretation
- Decision log append

## Recommendation
GO for implementation of Task 1-3 in order.
