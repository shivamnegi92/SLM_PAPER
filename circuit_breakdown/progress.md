# Post-Run Progress

## Second Diagnostic Wave Complete (2026-09-08)

- Status check at 09:49 local found no active model jobs. The guard/rate pilot
	had completed all 32 trajectories the previous evening; cancelling the monitor
	did not stop the model run. No guard/rate rerun was needed.
- Revalidated every case, configuration, guard record, source hash and exact
	summary/report. Track8 at 128 steps: shrink 1/4 versus observe 3/4 at both
	learning rates; full-space 4/4 in all cells. Paired intervals are wide and
	observe-only raises monitored disagreement. G1 remains open.
- Continued the already-authorized next step: baseline-context pilot completed
	all 128 planned responses with built-in token/source/response/statistic checks.
- Zero-swap correct counts none/prior/fresh_a/fresh_b: 1/3/10/2 out of 16.
	One-swap counts: 16/15/16/16. Strict and parsed counts agree here. Context
	matters, but no-demonstration zero-swap results remain weak; G2 remains open.
- Full circuit-breakdown suite: 122 tests passed. Original core source and
	final evidence ledger match the previous hashes. Both second-wave pilots
	are complete; other reviewer gaps are not silently marked resolved.

## Second Diagnostic Wave Started (2026-09-07)

- User authorized the next guard/optimizer and baseline-control checks.
- No model jobs were active at preflight. Historical sources and results will remain unchanged.
- Declared guard_rate_pilot_v1: four seed-2 development pairs, full/track8,
	shrink/observe guard, original/quarter rate, 32/64/128 checkpoints; 32 trajectories.
- Guard wrapper reuses the unchanged trajectory kernel. Factorial isolation,
	observe-only semantics and altered-event rejection checks passed.
- Saved G2 no-swap swap/in_box responses: 14/16 answers were H despite balanced
	targets. This is evidence for a context diagnostic, not a proven context cause.
- Baseline-context CPU preflight found no overlap-free A/B pair under fixed
	adjacent-label pairing. Before model evaluation, declared deterministic cyclic
	pairings and object assignment to retain all labels/counts and the freshness
	constraint. No labels, outcomes, prompts or parser were selected using model feedback.
- A second CPU check showed insufficient unused no-swap wording even with
	reassigned counterfactual pairs. A no-model feasibility probe passed with cup/pen
	added to the query vocabulary. Declared that vocabulary and its generalization
	caveat before inference; all context arms receive the exact same fresh queries.
- Baseline-context data and execution checks now pass (seven tests). A small
	completeness-check ordering error was corrected before execution.
- Verified the actual 16-pair/128-response design against the original manifest,
	prior diagnostic prompts, demonstrations and Phi tokenization. No overlap or
	canonical answer-boundary mismatch remains; editor diagnostics are clear.
- Guard/rate model run is still the only GPU job; second baseline job waits.

## Latest Revision Check (2026-09-07 21:20 local)

- The first convergence pilot completed after the earlier monitor was cancelled.
	No active model worker remained at this check; no experiment restart was needed.
- Revalidated all 16 trajectory artifacts, source hashes and exact summary/report
	regeneration. Full-space 8/8 at all checkpoints; track8 1/8 at 32 steps and
	2/8 at 64 and 128. G1 is still open scientifically.
- Recorded 118 track8 guard shrink events versus 11 full-space events across
	eight trajectories each. This is a follow-up diagnostic, not causal attribution.
- G2 had not started at the check. Continuing the authorized sequential work
	with its already-declared 256-response, fresh-development design.

## Sequential G2 Completion (2026-09-07)

- Ran the declared swap-format pilot after validating G1 completion. It saved
	all 256 responses and generated its summary/report in about 3 minutes 21 seconds.
- Independently checked all cases, decoded text, token identities, source hashes,
	regenerated statistics and report. Original core source and ledger hashes match.
- One-swap exchange/is: strict 0/16, parsed 16/16. Three/four swaps under that
	same format: strict 0/16, parsed 7/16. Format contributes in this diagnostic,
	but harder-task competence is not established and old failures are retained.
- Zero-swap sanity controls are weak; do not interpret the new data as a clean
	monotonic depth curve. G2 remains a development diagnostic.
- Full subproject regression suite: 107 tests passed. Next G1/G2 controls are
	documented; no additional model job was launched after this pilot.

## 2026-09-07

- Scope approved: evidence audit, documentation, detailed reporting, reproduction.
- Read public-only compliance, current collector/analyzer, and neighboring tests.
- Existing unrelated worktree changes will be preserved.
- Added the first read-only paired-summary auditor and focused regression checks.
- Validation of this first slice is next; no new model runs started.
- Initial interval tuple/list mismatch repaired. Three focused tests passed.
- All six real paired summaries reproduced; no numerical mismatch found.
- Added capability checks for item predictions, paired statistics, window
	losses, zero controls and matched norms; next run the expanded focused suite.
- Eight focused audit tests passed; all nine real capability outputs passed.
- Added tokenizer-only identity validation, per-case/configuration checks,
	head-statistic rebuilding, baseline-only checks and a non-overwriting CLI.
- Thirteen audit tests passed. Full CPU-only audit passed for all three models,
	including all condition/case checkpoints, nine capability runs, three head
	runs, nine baseline-only runs and equality of the snapshot with its sources.
- Added a detailed reporter and snapshot-checksum binding. Original snapshots
	remain immutable; the first audit report is retained as an intermediate record.
- Four reporting tests passed. Bound audit saved as consistency.json and
	paper/RESULTS_DETAILS.md generated from the original snapshot.
- Updated active plan, handoff and README; obsolete execution-status check passed.
- Updated manuscript framing/units and original prose formatter, added verified
	Makelov/HellaSwag citations, linked detailed evidence, marked legacy narratives retired.
- Collector percentage-point rendering regression passed after local indentation repair.
- Four reproduction-capture tests passed, including same-size byte tampering and .bin coverage.
- Full circuit-breakdown suite: 85 tests passed. Checked editor diagnostics are clear.
- Existing control figure opened and inspected; original image retained unchanged.
- Captured and verified 3,190 project/code/data/result files and 33 active root
	assets across three model inventories. Current environment pins were saved.
- Verified the original ledger checksum, exact manuscript generated-region
	agreement, and exact detailed-table regeneration.
- Parsed 13 documents with the installed Markdown parser: all 124 local links
	and code-fence checks passed. Correctly rooted tracked-doc whitespace check passed.
- Steps 1-4 complete. No new pretrained-model runs or original-result changes.
	Author review, anonymous release preparation, final PDF formatting and AI-use
	attestation remain explicit submission tasks, not silently marked complete.

## Reviewer Revision Started (2026-09-07)

- User authorized a tracked response to the expert review and sequential work.
- Added REVIEWER_GAP_TRACKER.md with established evidence, seven gaps, concrete
	follow-ups and resolution gates; linked it from the active pending plan.
- Declared convergence_pilot_v1 before new measurements: Phi transfer, eight
	seed-1 development pairs, full/track8, checkpoints 32/64/128, original rates.
- Checked for existing study workers; none matched the current process check.
- No historical results or frozen source files changed. Pilot implementation
	and focused validation follow before loading model weights.
- G1 implemented with a separate kernel/runner. Eleven tests and editor checks
	pass; the MPS pilot is running with one process and durable per-case outputs.
- Terminal execution returned loading output without a completion marker;
	an isolated process check confirmed the worker was alive. No duplicate job
	was started and the shared terminal was left undisturbed.
- G2 prepared while the first model job runs, without parallel model compute:
	fresh balanced simulator cases, strict/parsed score separation, raw generation
	and next-token logits, preflight source/model identity, and 256-response cap.
- G2's eleven focused tests pass. CPU preflight confirms no old-prompt overlap
	and all 256 Phi canonical answer boundaries align. It waits for G1 completion.