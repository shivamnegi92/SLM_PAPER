# Post-Run Evidence and Paper Work

## Completed Post-Run Scope

Audit the completed declared study, update stale claims, finish human-readable
tables, and record reproduction details. Do not start new model experiments,
change frozen experiment code, overwrite original result snapshots, commit,
or publish externally. All content remains public-data-only.

## Phases

1. Audit raw records, saved summaries, capability controls, and provenance.
   Status: complete
2. Generate detailed tables and reconcile paper/plan claims.
   Status: complete
3. Capture checksums, environment, reproduction commands, and submission caveats.
   Status: complete
4. Run focused validation and report remaining scientific limitations.
   Status: complete

## First Discriminating Check

Hypothesis: the stored paired summary is reproducible from the saved per-example
records using the existing analyzer. A deliberately altered summary rate must
fail the read-only auditor. A matching fixture must pass.

## Constraints

- Preserve unrelated worktree changes and all original experimental artifacts.
- Keep failed competence and capability criteria visible.
- Do not interpret per-example target override as a reusable editor.
- Write new audit/reproduction outputs to a separate, non-overwriting directory.

## Errors

- First focused audit check: JSON lists differed from in-memory interval tuples.
   Normalize sequence types in the comparison, retaining strict values/lengths;
   rerun the same three tests before expanding the auditor.
- Collector unit-formatting edit introduced indentation drift; repaired the
   exact block with full context. Both focused collector tests then passed.

## Final Verification

- Full subproject suite: 85 tests passed.
- Original ledger checksum still matches the bound audit.
- Manuscript result region and detailed tables exactly match their generators.
- 3,190 project files and active assets for all three models were hashed and
   verified; .bin weights are included. These are post-run identities only.
- 124 local links/code-fence checks passed across 13 documents.
- Edited code/document diagnostics are clear; original figure inspected.
- No original experiment outputs overwritten, no new model runs, no commits
   or external submission. Formatting, anonymity, AI-use attestation and human
   author review remain outside this completed post-run work.

## Authorized Reviewer-Revision Phase (2026-09-07)

The user now requests documentation of all reviewer gaps and sequential work
on them. This supersedes the prior no-new-experiment scope for new, separately
versioned development pilots only. Never change the frozen experiment source,
historical results or original reproduction capture.

1. Document evidence, seven gaps, acceptance criteria and dependency order.
   Status: complete
2. Implement and validate a bounded development-only convergence pilot (G1).
   Status: complete
3. Verify model/source/protocol provenance, execute the pilot and analyze its
   declared checkpoints; report whether G1 remains unresolved.
   Status: complete
4. Record the next discriminating step, then proceed to G2 without claiming
   all scientific gaps are closed.
    Status: complete

### First Revision Pilots Complete

- G1: 16/16 trajectories verified; full-space 8/8 at 32/64/128 steps,
   track8 1/8, 2/8, 2/8. Scientific gap still open.
- G2: 256/256 fresh-development responses completed and verified. `is` cues
   expose first-token format failures, but multi-swap and zero-swap competence
   remain inadequate. No new causal-control claim.
- All 107 subproject tests passed; original core source and original ledger
   still match their recorded hashes.
- Next diagnostic protocols are listed in REVIEWER_GAP_TRACKER.md. This marks
   the initial pilot work complete, not the seven-gap research program.

First hypothesis: the selected Phi transfer rank8 optimizer may not be stable
at 32 steps. Cheap disconfirmation: on eight fixed seed-1 development pairs,
compare 32/64/128-step snapshots of the same trajectory for full and track8.
Before a model run, require a CPU fixture to reproduce the unchanged 32-step
optimizer and prove snapshot vectors/logits are saved at the declared step.

Full gap list and scope gates: [REVIEWER_GAP_TRACKER.md](REVIEWER_GAP_TRACKER.md).

## Second Diagnostic Wave (Authorized 2026-09-07)

1. Declare and validate guard-by-rate controls using the unchanged trajectory kernel.
   Status: complete
2. Run the fixed 32-trajectory factorial on four seed-2 development pairs.
   Status: complete
3. Diagnose baseline context effects on fresh no-swap and one-swap examples
   with fixed answer cue and independently generated demonstrations.
   Status: complete
4. Validate source/result integrity and report both outcomes without closing
   their parent scientific gaps prematurely.
   Status: complete

G1 hypothesis: repeated guard shrinking, not just optimization duration,
limits target override. Check: guard shrink/observe crossed with original/quarter
rate for full/track8, paired on the same four development examples at 128 steps.
Both arms still measure guard agreement; the observe-only arm is not called safe.

G2 hypothesis: fixed few-shot context contributes to the no-swap answer collapse
(14/16 answers were H in the original swap/in_box sanity condition). Check:
fresh balanced no-swap/one-swap cases with no demonstrations and two independently
specified demonstration sets, at a fixed answer cue and unchanged parser.

The prior demonstration prefix is included as a fourth matched context. CPU
freshness checks required adding cup/pen to the query vocabulary before inference;
all context conditions still receive the same questions and balanced target labels.
Baseline-context implementation has seven passing tests and 128 aligned answer
boundaries. Execution completed on 2026-09-08 after verifying the guard/rate run.

### Second-Wave Verification (2026-09-08)

- Guard/rate: all 32 trajectory files, recorded configurations, guard scores,
   source hashes and report regenerated successfully. At 128 steps track8 is
   1/4 with shrink enforcement and 3/4 observe-only at both rates; full is 4/4.
   Observe-only also increases monitored disagreement; no safety claim follows.
- Baseline context: all 128 responses and paired summaries verified. Zero-swap
   correct counts are 1/3/10/2 out of 16 across none/prior/fresh_a/fresh_b;
   one-swap counts are 16/15/16/16. Context sensitivity does not establish
   multistep competence or show demonstrations are the sole cause.
- All 122 subproject tests pass. Original frozen source and result snapshot
   hashes are unchanged. G1/G2 remain scientifically open; G3-G6 are queued.